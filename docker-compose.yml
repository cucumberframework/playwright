version: '3.8'

services:
  playwright-tests:
    build: .
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - CI=true
      - HEADLESS=true
      - BROWSER=chromium
    command: npm test